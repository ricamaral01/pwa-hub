# Manifesto de arquivos historicos

O manifesto fica em `../imports/historico/manifesto-arquivos-esperados.json`.

Estado atual: nenhum arquivo historico real foi fornecido. Existe somente fixture CSV em `../imports/historico/exemplos` para validar o pipeline.

Quando os arquivos reais forem entregues, o manifesto deve listar nome esperado, origem, entidade alvo, obrigatoriedade e observacoes de layout.


(function(){const form=document.getElementById('loginForm'),msg=document.getElementById('message'),toggle=document.querySelector('.password-toggle'),password=document.getElementById('password');toggle.onclick=()=>{password.type=password.type==='password'?'text':'password';toggle.textContent=password.type==='password'?'👁':'🙈'};form.onsubmit=async e=>{e.preventDefault();msg.className='auth-message';const button=form.querySelector('button[type=submit]');button.disabled=true;try{const result=await ConcretrackAuth.call('auth-login',{email:form.email.value,password:form.password.value});await ConcretrackAuth.client.auth.setSession({access_token:result.session.access_token,refresh_token:result.session.refresh_token});const next=new URLSearchParams(location.search).get('next');location.replace(result.user.first_login?'/change-password.html':(next&&next.startsWith('/')?next:'/'))}catch(error){msg.textContent=error.message;msg.className='auth-message error show'}finally{button.disabled=false}}})();
